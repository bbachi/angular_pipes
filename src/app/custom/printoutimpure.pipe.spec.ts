import { PrintoutimpurePipe } from './printoutimpure.pipe';

describe('PrintoutimpurePipe', () => {
  it('create an instance', () => {
    const pipe = new PrintoutimpurePipe();
    expect(pipe).toBeTruthy();
  });
});
